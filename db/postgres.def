Bootstrap: docker
  From: postgres:alpine

  %post
      # Remove privileged startup scripts
      rm -f /usr/local/bin/docker-entrypoint.sh

  %runscript
      # Simple postgres startup without user switching
      postgres -D /var/lib/postgresql/data
